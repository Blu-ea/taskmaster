{
  "langrev": "1",
  "warnings": [ "no-unused" ],
  "dependency-search-paths": [ "lib" ],
  "dependencies": [ ],
  "authors": [ "Blu-ea", "Myresa" ],
  "version": "0.1.0",
  //"sources": [ "src/**" ],
  "test-sources": [ "test/**" ],
  "build-dir": "build",
  "output": "build",
  // Architecture and OS target.
  // You can use 'c3c --list-targets' to list all valid targets.
  // "target": "windows-x64",
  // Targets.
  "targets": {
    "TaskMaster": {
      "type": "executable",
      "sources": [ "src/**" ]
    },
    "TaskMasterCli": {
      "type" : "executable",
      "sources": [ "src_cli/**" ]
    }
  },
  "cpu": "generic",
  "opt": "O0",
  "safe": true,
  "sanitize": "none"
}